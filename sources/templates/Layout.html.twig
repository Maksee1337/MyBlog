<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">


        <!-- Bootstrap core CSS -->
        <link href="../../css/bootstrap.min.css" rel="stylesheet">
{#        <link href="https://fonts.googleapis.com/css?family=Playfair&#43;Display:700,900&amp;display=swap" rel="stylesheet">#}
        <link href="../../css/blog.css" rel="stylesheet">
    </head>
    <body>
        <div class="container">
            <header class="blog-header py-3">
                <div class="row flex-nowrap justify-content-between align-items-center">
                    <div class="col-8 d-flex  justify-content-start align-items-start">
                        {% set route = app.request.attributes.get('_route') %}
                        {% set route_params = app.request.attributes.get('_route_params') %}
                        {% set params = route_params|merge(app.request.query.all) %}

                        {# проблема выскочила неожиданно, хотя она скорее всего была и раньше, надо было тесты запускать #}
                        <a  href="{{ path(route, params|merge({ _locale: 'en' })) }}">{{ 'EN' }}</a>
                        <a  href="{{ path(route, params|merge({ _locale: 'ru' })) }}">{{ 'RU' }}</a>

                    </div>
                    <div class="col-4 d-flex justify-content-end align-items-center">
                        {% if app.user %}
                            Hello, {{ app.user }}
                            <a class="btn btn-sm btn-outline-secondary" href="{{ path('logout') }}">Logout</a>
                        {% else %}
                            <a class="btn btn-sm btn-outline-secondary" href="{{ path('User_Login') }}">Login</a>
                        {% endif %}
                    </div>
                </div>
            </header>

            <div class="nav-scroller py-1 mb-2">
                <nav class="nav d-flex justify-content-start">
                    <a class="p-2 link-secondary" href="{{ path('Default_Index') }}">{{ 'Homepage'|trans }}</a>

                    <a class="p-2 link-secondary" href="{{ path('News_NewPost') }}">{{ 'New Post'|trans }}</a>
                    <a class="p-2 link-secondary" href="{{ path('Default_About') }}">{{ 'About'|trans }}</a>
                    <a class="p-2 link-secondary" href="{{ path('Default_Feedback') }}">{{ 'Feedback'|trans }}</a>
                    <a class="p-2 link-secondary" href="https://instagram.com" target="_blank">Instagram</a>
                    <a class="p-2 link-secondary" href="https://facebook.com" target="_blank">Facebook</a>
                </nav>
            </div>
        </div>
        {% block body %}
        {% endblock %}
        <footer class="blog-footer">
            <p>Blog template built for <a href="https://getbootstrap.com/">Bootstrap</a> by <a href="https://twitter.com/mdo">@mdo</a>.</p>
            <p>
                <a href="#">Back to top</a>
            </p>
        </footer>
    </body>
</html>
