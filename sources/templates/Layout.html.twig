<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">


        <!-- Bootstrap core CSS -->
        <link href="../css/bootstrap.min.css" rel="stylesheet">
{#        <link href="https://fonts.googleapis.com/css?family=Playfair&#43;Display:700,900&amp;display=swap" rel="stylesheet">#}
        <link href="../css/blog.css" rel="stylesheet">
    </head>
    <body>
        <div class="container">
            <header class="blog-header py-3">
                <div class="row flex-nowrap justify-content-between align-items-center">
                    <div class="col-8 d-flex  justify-content-start align-items-start">
                        Logo
                    </div>
                    <div class="col-4 d-flex justify-content-end align-items-center">
                        <a class="btn btn-sm btn-outline-secondary" href="#">Sign up</a>
                    </div>
                </div>
            </header>

            <div class="nav-scroller py-1 mb-2">
                <nav class="nav d-flex justify-content-start">
                    <a class="p-2 link-secondary" href="{{ path('Default_Index') }}">Homepage</a>

                    <a class="p-2 link-secondary" href="{{ path('News_NewPost') }}">New Post</a>
                    <a class="p-2 link-secondary" href="{{ path('Default_About') }}">About</a>
                    <a class="p-2 link-secondary" href="{{ path('Default_Feedback') }}">Feedback</a>
                    <a class="p-2 link-secondary" href="https://instagram.com" target="_blank">Instagram</a>
                    <a class="p-2 link-secondary" href="https://facebook.com" target="_blank">Facebook</a>
                </nav>
            </div>
        </div>
        {% block body %}
        {% endblock %}
        <footer class="blog-footer">
            <p>Blog template built for <a href="https://getbootstrap.com/">Bootstrap</a> by <a href="https://twitter.com/mdo">@mdo</a>.</p>
            <p>
                <a href="#">Back to top</a>
            </p>
        </footer>
    </body>
</html>
